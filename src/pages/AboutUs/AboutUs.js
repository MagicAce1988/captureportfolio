import { AboutSection, ServicesSection, FaqSection } from '../../components';

const AboutUs = ({ ...props }) => {
  return (
    <>
      <AboutSection />
      <ServicesSection />
      <FaqSection />
    </>
  );
};

export default AboutUs;
